<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="GreenHouse.Mobile.Views.ReportsPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name ="AddReport" Icon="search.png" Text="+"/>
    </ContentPage.ToolbarItems>
    <ListView ItemsSource="{Binding Reports}"
              HasUnevenRows="True"
              BackgroundColor="#ecf0f1"
              SeparatorVisibility="None"
              HorizontalOptions="FillAndExpand"
              VerticalOptions="FillAndExpand">

        <ListView.ItemTemplate>

            <DataTemplate>
                <ViewCell>
                    <StackLayout Margin="6,4,6,4"
                                 BackgroundColor="White">
                        <Label Text="{Binding StarsOn}"
                               Margin="8"
                               FontSize="Medium" />
                        <Label Text="{Binding IsScheduled}"
                               Margin="8,2,8,4"
                               FontSize="Medium" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>

        </ListView.ItemTemplate>
    </ListView>
</ContentPage>